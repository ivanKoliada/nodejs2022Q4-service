# version: '3.9'

# services:
  # api:
  #   container_name: api
  #   build:
  #     dockerfile: ./Dockerfile
  #     context: .
  #   volumes:
  #     - .:/app
  #     - node_modules:/app/node_modules
  #   restart: always
  #   env_file:
  #     - .env
  #   ports:
  #     - ${PORT}:${PORT}
  #   depends_on:
  #     - database
  #   networks:
  #     - docker-network
  # database:
  #   container_name: database
  #   build:
  #     dockerfile: ./src/database/Dockerfile
  #     context: .
    # volumes:
    #   - database_files:/var/lib/postgresql/data
    #   - database_logs:/var/log/postgresql/postgresql.log
    # restart: always
    # env_file:
    #   - .env
    # ports:
    #   - ${POSTGRES_PORT}:${POSTGRES_PORT}
    # networks:
    #   - docker-network

# volumes:
#   node_modules:
#   database_files:
#   database_logs:

# networks:
#   docker-network:
#     driver: bridge

version: '3.8'

services:
  db:
    container_name: db
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_PASSWORD=root
      - POSTGRES_USER=admin
    image: postgres:alpine3.17